#include <stdio.h>

int main()
{
    int n;
    printf("ใส่ตัวเลข : ");
    scanf("%d", &n);

    if (n % 2 == 0) {
        printf("%d เลขคู่ ", n);
    } else {
        printf("%d เลขคี่ ", n);
    }
}

